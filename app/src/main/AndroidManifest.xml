<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.notadeveloper.app.pat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:supportsRtl">
        <activity
            android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity
            android:name=".MainActivity"
            />
        <activity android:name=".detailedpartyactivity" />
        <activity android:name=".DetailedTicketActivity" />
        <activity android:name=".MyTickets" />
        <activity android:name=".SheeshaActivity" />
        <activity android:name=".SheeshaOrderActivity" />
        <activity android:name=".ClubDetailedActivity" />
        <activity android:name=".AboutActivity"/>
    </application>

</manifest>